- if @post.has_liked?(current_user.id)
  = link_to post_like_path(@post.id, id: @post.likes[0].id), method: :delete, remote: true do
    = image_tag 'after-like.svg'
- else
  = link_to post_likes_path(@post.id), method: :post, remote: true do
    = image_tag 'before-like.svg'
= @post.likes.count
